---
description: Update CLAUDE.md and README.md based on recent changes
---

Look at the changes made in this session (or run `git diff main --stat` and 
`git log -5 --oneline` if unsure what changed) and update the project docs:

1. **CLAUDE.md** — update:
   - Architecture/module overview if new components were added
   - Build/run/test commands if they changed
   - Conventions or patterns introduced (e.g. new transaction patterns, 
     new entity relationships)
   - Known gotchas or decisions worth remembering

2. **README.md** — update:
   - Feature list
   - Setup/installation steps if dependencies changed
   - API endpoint list if new controllers were added
   - Environment variables if new config was introduced

Keep both files concise. Don't restate things that haven't changed. 
Show me a diff-style summary of what you changed before finalizing.